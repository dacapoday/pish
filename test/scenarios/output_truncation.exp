#!/usr/bin/expect -f
set timeout 15
catch {unset env(PISH_PID)}
spawn node [file dirname [info script]]/../../dist/main.js
expect -re {PISH_READY} {}
sleep 0.1
send "seq 1 250\r"
expect -re {PISH_READY} {}
sleep 0.1
send "exit\r"
expect eof
